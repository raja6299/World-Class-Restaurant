# Architecture

> Auto-generated by /map on 2026-06-29

## Overview

Aurum Restaurant is a Next.js (App Router) web application providing a world-class restaurant digital experience. It includes pages for the menu, gallery, experiences, and a home page. It is built using React, Tailwind CSS, Framer Motion for animations, and Firebase for backend services (likely for reservations, dynamic menus, or gallery data).

```text
┌─────────────────────────────────────────┐
│          Next.js App Router             │
│        (app/, page.tsx, layout.tsx)     │
├─────────────────────────────────────────┤
│            Components Layer             │
│   (components/home, layout, shared)     │
├─────────────────────────────────────────┤
│    Business Logic & Hooks (lib/hooks)   │
├─────────────────────────────────────────┤
│      Data Layer (lib/firebase)          │
└─────────────────────────────────────────┘
```

## Components

### App Routing (`app/`)
- **Purpose:** Handles the routing for the application.
- **Location:** `app/`
- **Dependencies:** React, Next.js.
- Contains routes for `(site)/experiences`, `(site)/gallery`, `(site)/menu`.

### UI Components (`components/`)
- **Purpose:** Reusable UI components.
- **Location:** `components/`
- **Structure:** Divided into `home`, `layout` (navbar, footer), and `shared` (buttons, modals, etc.).

### Firebase Integration (`lib/firebase/`)
- **Purpose:** Connects to Firebase for data persistence and authentication.
- **Location:** `lib/firebase/`

### Utilities & Types (`lib/`)
- **Purpose:** Shared utility functions (`lib/utils`) and TypeScript type definitions (`lib/types`).
- **Location:** `lib/`

## Data Flow

1. User visits a page (e.g., `/menu`).
2. Next.js App Router serves the server/client components.
3. Components fetch data (if dynamic) using hooks in `lib/hooks` or directly from `lib/firebase`.
4. Interactive elements are animated via Framer Motion.
5. User inputs (e.g., reservations) are handled via React Hook Form and submitted to Firebase.

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| Firebase | BaaS | Database (Firestore), Auth, or Storage for the restaurant data. |

## Technical Debt

- [ ] Complete setup of Firebase environment variables in `.env.local`
- [ ] Ensure all pages are fully responsive.

## Conventions

**Naming:** PascalCase for components, camelCase for utilities and hooks.
**Structure:** Next.js App Router convention (`app/`), atomic-like design for components (`components/`).
**Styling:** Tailwind CSS utility classes.
